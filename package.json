{
  "name": "test_demo",
  "version": "1.0.0",
  "description": "short demo on toxicity classification",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "info": "echo 'npm as a build tool'",
    "prep": "mkdir dist && npm install budo -g",
    "start": "budo main.js:dist/bundle.js --live --host localhost"
  },
  "keywords": [
    "tfjs"
  ],
  "author": "Ryan Liwag",
  "license": "ISC",
  "dependencies": {
    "budo": "^11.6.2"
  }
}
